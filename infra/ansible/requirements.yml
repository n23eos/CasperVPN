---
# Ansible Galaxy collections needed by the node roles.
collections:
  - name: ansible.posix     # sysctl, firewalld helpers
    version: ">=1.5.0"
  - name: community.general # misc modules
    version: ">=8.0.0"
